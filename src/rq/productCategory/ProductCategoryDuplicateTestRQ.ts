/**
 * Product category duplicate test request data
 * 产品分类重复测试请求数据
 */
export type ProductCategoryDuplicateTestRQ = {
  /**
   * Excluded id
   * 排除的编号
   */
  excludedId?: number;

  /**
   * Name
   * 名称
   */
  name?: string;

  /**
   * Assigned id
   * 分配编号
   */
  assignedId?: string;
};
